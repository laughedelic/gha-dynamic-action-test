on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@2

      - run: echo 'test'
      
      - uses: ./.github/actions/a

      - uses: ./.github/actions/b
